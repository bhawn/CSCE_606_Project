{
  "name": "src_mario",
  "version": "1.0.0",
  "description": "1) npm init 2) get parcel bundler 3) npm install kaboom",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html -p 8000",
    "build": "parcel build src/index.html --no-scope-hoist"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kaboom": "^2000.2.9"
  },
  "alias": {
    "kaboom": "./node_modules/kaboom/dist/kaboom.mjs"
  }
}
